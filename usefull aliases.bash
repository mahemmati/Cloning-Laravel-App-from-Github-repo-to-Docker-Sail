alias sail="bash vendor/bin/sail"
alias s="bash vendor/bin/sail"

alias up="bash vendor/bin/sail up -d"
alias u="bash vendor/bin/sail up -d"

alias down="bash vendor/bin/sail down"
alias d="bash vendor/bin/sail down"

alias artisan="bash vendor/bin/sail artisan"
alias a="bash vendor/bin/sail artisan"

alias mkmd="bash vendor/bin/sail artisan make:model"
alias mkmd-a="bash vendor/bin/sail artisan make:model --all"

alias mkcp="bash vendor/bin/sail artisan make:component"
alias mkcpvw="bash vendor/bin/sail artisan make:component --view"

alias mg="bash vendor/bin/sail artisan migrate"
alias mgfr="bash vendor/bin/sail artisan migrate:fresh"
alias mgfrsd="bash vendor/bin/sail artisan migrate:fresh --seed"

alias test="bash vendor/bin/sail artisan test"
alias ts="bash vendor/bin/sail artisan test"
